class Solution:
    def findEquilibrium(self, arr):
        total_sum = sum(arr)
        left_sum = 0

        for i in range(len(arr)):
            total_sum -= arr[i]   # right side sum

            if left_sum == total_sum:
                return i          # equilibrium index

            left_sum += arr[i]

        return -1
